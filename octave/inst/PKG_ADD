## PKG_ADD: autoload and initialization for volesti package
##
## This file is automatically generated and will be sourced when the package
## is loaded.

## Add package to Octave path
if (exist ("volesti", "dir"))
  addpath (genpath (fullfile (pkg ("list", "volesti").dir{1}, "inst")));
endif

## Display package information on load
if (isempty (pkg ("list", "volesti")))
  warning ("VolEsti package not properly installed");
else
  volesti ();
endif

