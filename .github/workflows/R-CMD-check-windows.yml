##############################################################################
# GitHub Actions Workflow to test the R interface of volesti
#
# Copyright (c) 2020 Vissarion Fisikopoulos
#
# Licensed under GNU LGPL.3, see LICENCE file
##############################################################################

on: [push, pull_request]

name: R-CMD-check-windows

jobs:
  R-CMD-check:
    runs-on: ${{ matrix.config.os }}

    name: ${{ matrix.config.os }} (${{ matrix.config.r }})

    strategy:
      fail-fast: false
      matrix:
        config:
#           - {os: windows-latest, r: 'devel'} # Error in library(devtools) : there is no package called ‘devtools’
          - {os: windows-latest, r: 'release'}

    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
      R_REMOTES_NO_ERRORS_FROM_WARNINGS: true
      _R_CHECK_FORCE_SUGGESTS_: false
      RSPM: ${{ matrix.config.rspm }}
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.config.r }}

      - uses: r-lib/actions/setup-pandoc@v2

      - name: Install dependencies
        run: Rscript -e "install.packages(c('rcmdcheck', 'Rcpp', 'RcppEigen', 'BH', 'testthat'), dependencies = TRUE, repos = 'https://cloud.r-project.org')"

      - name: Checkout Rvolesti repository
        run: git clone https://github.com/GeomScale/Rvolesti.git

      - name: Upgrade with current develop of volesti
        shell: bash
        run: |
          mkdir -p Rvolesti/src/volesti
          cp -rf include Rvolesti/src/volesti/
          cp -rf external/arpack++ Rvolesti/src/external/
          cp -rf external/ChebTools Rvolesti/src/external/
          cp -rf external/cmake-files Rvolesti/src/external/
          cp -rf external/minimum_ellipsoid Rvolesti/src/external/
          cp -rf external/PackedCSparse Rvolesti/src/external/
          cp -rf external/Padua Rvolesti/src/external/

      - name: Check
        env:
          _R_CHECK_CRAN_INCOMING_REMOTE_: false
        run: |
          cd Rvolesti/
          Rscript -e "library(rcmdcheck)" -e "rcmdcheck::rcmdcheck(args = c('--no-manual'), error_on = 'warning', check_dir = 'check')"

      - name: Upload check results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ runner.os }}-r${{ matrix.config.r }}-results
          path: Rvolesti/check
